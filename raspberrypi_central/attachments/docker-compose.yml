version: "3"
services:
    py:
        build: ./app
        command: pipenv run python alarm.py
        env_file:
            - ./.env

networks:
    default:
        external: 
            name: webapp_webapp_backend
